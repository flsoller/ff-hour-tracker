name: Database_Migrator

on:
  workflow_dispatch:
    inputs:
      db_conn_string_key:
        required: true
  workflow_call:

jobs:
  migrate_db:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
