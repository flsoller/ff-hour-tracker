{
  "name": "@hour-tracker/lambda-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "esbuild --outdir=dist src/*/*.ts src/*.ts --bundle --sourcemap --platform=node --target=es2020 --packages=external"
  },
  "description": "Support package for shared lambda api code",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/jest": "29.5.12",
    "esbuild": "0.21.4",
    "jest": "29.7.0",
    "ts-jest": "29.1.4",
    "tsx": "4.11.0",
    "typescript": "5.4.5"
  }
}
